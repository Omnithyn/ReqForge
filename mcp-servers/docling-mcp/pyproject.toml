[project]
name = "reqforge-docling-mcp"
version = "0.1.0"
description = "ReqForge Docling MCP Server — 企业文档解析 (PDF/Word/Excel/PPT)"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.0",
    "docling>=2.0",
]

[project.scripts]
docling-mcp = "server:main"
